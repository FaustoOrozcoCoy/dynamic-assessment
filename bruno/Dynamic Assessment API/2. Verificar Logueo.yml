info:
  name: 2. Verificar Logueo
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/auth/me"
  auth:
    type: bearer
    token: "{{student_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debería retornar un código 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("Debería retornar un JSON", function() {
          const data = res.getBody();
          expect(data).to.be.a('object'); 
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
